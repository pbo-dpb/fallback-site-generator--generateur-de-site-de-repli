<header>
   <section class="container mx-auto px-4">
      <div class="flex flex-col-reverse md:flex-row md:items-center py-2 md:py-4 md:justify-between w-full">
         <a href="/{{ language }}/" class="w-full md:w-1/4 place-self-center"><img src="/logo.full.{{ language }}.svg" alt="{{ strings.title }}" class="w-full"></a>
         <div class="text-right md:w-1/4">
            <div class="md:mb-4">
            <nav>
               <a 
               href="{% if langalt is defined %}{{ langalt }}{% else %}/{{ language == 'en' ? 'fr' : 'en' }}/{% endif %}" 
               class="text-blue-800 underline">
               <span class="hidden md:block">{{ language == 'en' ? 'Français' : 'English' }}</span><span class="md:hidden">{{ language == 'en' ? 'FR' : 'EN' }}</span></a>
           </nav>
            </div>
            
         </div>
      </div>
   </section>
  
</header>
{% include 'breadcrumbs.twig' %}
